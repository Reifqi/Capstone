const dogHealth = {
  async render() {
    return `
          <h2>Health Page(Dog)</h2>
        `;
  },

  afterRender() {
    // Fungsi ini akan dipanggil setelah render()
  },
};

export default dogHealth;
