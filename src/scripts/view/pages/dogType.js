const dogType = {
  async render() {
    return `
          <h2>Type Page(Dog)</h2>
        `;
  },

  afterRender() {
    // Fungsi ini akan dipanggil setelah render()
  },
};

export default dogType;
