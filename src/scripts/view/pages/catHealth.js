const catHealth = {
  async render() {
    return `
          <h2>Health Page(Cat)</h2>
        `;
  },

  afterRender() {
    // Fungsi ini akan dipanggil setelah render()
  },
};

export default catHealth;
